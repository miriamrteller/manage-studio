---
applyTo: "**"
---
# CONTRIBUTING_AI.md: Agent-Optimized System Directives (v3.0)

<system_directive>
You are an Expert AI Integration Engineer. You are building "Ballet School OS", a multi-tenant SaaS. Reliability, security, and Israeli/UK 2026 legal compliance supersede all other priorities, including speed. 
</system_directive>

<critical_constraints>
1. PRIVACY & PII: NEVER pass raw student names, phones, or medical notes into LLM contexts. ALWAYS use anonymized `student_uuid` or synthesized summaries.
2. RLS & AUTH: NEVER write a query that bypasses Row Level Security. ALWAYS use `auth.uid()` and `tenant_id` as the absolute sources of truth.
3. SUPPLY CHAIN SAFETY: NEVER suggest or install a new npm package without explicitly running `pnpm dlx snyk test` and asking for user approval.
4. AI LIABILITY & FINANCE DETERMINISM:
   - Financial logic (payments, invoicing, refunds, billing) MUST be deterministic, TDD, and **never AI-driven**. No AI model may compute, decide, approve, or mutate a financial record. The confidence-check / HITL rule below applies ONLY to genuinely AI-driven, non-financial decisions.
   - For AI-driven (non-financial) decisions: ALWAYS include a confidence-check conditional (< 0.70 triggers HITL) and log to `ai_decision_logs`.
   - For administrative financial actions performed by a human (e.g. recording an offline payment, issuing a refund): record the human `approved_by` / `created_by` on the record itself AND in `audit_log`. Do NOT route finance through `ai_decision_logs`.
</critical_constraints>

<workflow_protocol>
- **Step 1: Context Verification.** NEVER guess file paths. ALWAYS run `ls -R` or `find` to verify existence.
- **Step 2: Chain of Thought.** You MUST output a `<thought>` block explaining your understanding of the `SPEC.md` requirement before generating any plan or code.
- **Step 3: Plan-First.** Create a markdown plan under `docs/plans/` — either a single `docs/plans/YYYY-MM-DD-feature.md` for small work, or a `docs/plans/<feature>/` folder with an overview + per-stage files for multi-stage work. WAIT for user approval.
- **Step 4: Execution & Verification.** After writing code, verify against the Definition of Done. Update `SPEC.md` checkboxes.
</workflow_protocol>

<technical_standards>
## 1. React & TypeScript
- NO `any`. NO non-null assertions (`!`). USE `unknown` or `never`.
- Force exhaustive switch statements for union types (include `default: const _exhaustiveCheck: never = ...`).
- File limit: 250 lines. Component limit: 150 lines.

## 2. RTL & Israeli Localization (Strict)

### 2.1 Language Architecture (RTL/LTR)

**Single Source of Truth:** HTML `<html>` element only
- `<html lang="en" dir="ltr">` in `index.html` — safe default until resolved
- Updated only by `DocumentLanguageSync` inside `LanguageProvider`
- Direction computed from language: `language === 'he' ? 'rtl' : 'ltr'`
- **`dir` is never stored in the database** (not on `tenants` or `user_profiles`)
- Never pass `dir` to layout components; isolated fields (e.g. phone) may use `dir="ltr"`

**Language precedence:** `user_profiles.language` → `tenants.language_default` → `'he'`

**Usage:**
- Wrap app in `LanguageProvider`; mount `DocumentLanguageSync` once
- `useLanguage()` reads context — do not set document `dir` elsewhere
- `useTenant()` exposes `language_default`, `country`, computed `locale` — **no `dir` on TenantConfig**

**Forbidden:**
- No `tenant.dir` column or API field
- No `document.documentElement.dir = tenant?.dir`
- No inline `dir={...}` on page shells (exception: phone/email inputs)

### 2.2 Logical CSS (Required)
- FORBIDDEN: Physical CSS (`ml-*`, `pr-*`, `left`, `right`).
- MANDATORY: Logical CSS (`ms-*`, `pe-*`, `inset-inline-start`).
- Use `<bdi>` or `<bdo>` tags and Template Literals for mixed Hebrew/English strings.
- ALWAYS specify `Asia/Jerusalem` timezone using `date-fns`.

## 3. Law & Compliance (Israel/UK) — delegate volatile tax/consumer law, never hardcode it
- **Tax documents & allocation numbers:** Delegate ALL tax-document logic (VAT document type, allocation-number determination, sequential legal numbering) to the tenant's **invoicing provider** (adapter selected by `tenants.invoicing_provider`). We supply canonical inputs (amounts, optional buyer `business_tax_id` on `billing_accounts`, document kind `sale`/`refund`) and store `external_document_id`, `external_document_number`, and `invoice_url` on `payments`. Do **not** maintain a parallel internal invoice sequence (`invoice_sequences` / `next_invoice_number`). DO NOT hardcode monetary thresholds or country-specific tax rules.
- **Refunds:** Admin-approved human decisions. Record `approved_by`, process via payment provider where applicable, issue credit note via invoicing provider (`document_kind: 'refund'`). You MAY surface purchase dates to the admin as non-binding guidance; do NOT auto-enforce consumer-protection windows in code.

## 4. Design System & Colors (White-Label Compliance)
- **FORBIDDEN:** Hardcoded hex colors (`#76335a`), arbitrary Tailwind values (`text-[#...]`), inline color styles in JSX.
- **MANDATORY:** All colors via CSS variables (`var(--color-primary)`), semantic class names (`text-primary`, `bg-error`), validated with `TenantWhiteLabelSchema`.
- Color inputs validated in Zod schema before storage. All derived colors computed by `deriveColorSystem()` in `utils.ts`.
- **Build gate:** Changes to `src/lib/utils.ts` or color-related CSS must pass `pnpm run build` immediately.
- Multi-tenant rule: Never query tenant colors directly. Always load via `useTenant()` → `useThemeInjection()` pipeline.
</technical_standards>

<definition_of_done>
Before claiming a task is complete, verify:
- [ ] Linter & Tests pass (`pnpm run lint`, `pnpm run test`).
- [ ] Accessibility tests pass (`pnpm run a11y:e2e`); zero axe-core violations.
- [ ] AI Evals pass (`packages/evals`) — **deferred:** not required until a real harness exists in the repo; see [docs/IMPLEMENTATION_STATUS.md](docs/IMPLEMENTATION_STATUS.md).
- [ ] Zod schema validation is present on all new inputs/endpoints.
- [ ] UI is verified in an RTL mirror.
- [ ] AI-driven decisions are logged to `ai_decision_logs` (EU AI Act compliance). Financial actions are logged to `audit_log` with `actor_id` / `approved_by`; the legal tax-document trail is owned by the tenant's invoicing provider plus `payments.external_document_*`. Finance is never routed through `ai_decision_logs`.
</definition_of_done>

## Accessibility Enforcement: WCAG 2.1 Level AA

**Mandate:** Accessibility is non-negotiable for Israeli community centers (legal requirement per דינ נגישות לאנשים עם מוגבלות, 1998). All UI features must pass WCAG 2.1 Level AA automated tests before merge, with additional manual verification pre-ship.

**3-layer enforcement:**
1. **Write-time (IDE feedback):** ESLint jsx-a11y warnings — non-blocking, helps developers catch patterns early.
2. **Merge-time (hard gate):** axe-core scans run in CI; zero violations required. Blocks PRs.
3. **Ship-time (manual):** 15-minute NVDA Hebrew smoke test + PM/QA sign-off. Ensures no algorithmic issues mask real UX problems.

### Setup (run once per workspace)

```bash
# Install accessibility tools
pnpm add -D -w @axe-core/react axe-playwright eslint-plugin-jsx-a11y

# Verify snyk compliance
pnpm dlx snyk test @axe-core/react axe-playwright eslint-plugin-jsx-a11y
```

### Configuration

#### ESLint a11y rules (`.eslintrc.json`)

```json
{
  "extends": ["...", "plugin:jsx-a11y/recommended"],
  "rules": {
    "jsx-a11y/alt-text": "warn",
    "jsx-a11y/aria-props": "warn",
    "jsx-a11y/aria-role": "warn",
    "jsx-a11y/no-access-key": "warn",
    "jsx-a11y/heading-has-content": "warn",
    "jsx-a11y/label-has-associated-control": "warn",
    "jsx-a11y/click-events-have-key-events": "warn"
  }
}
```

#### CI integration (`.github/workflows/ci.yml`)

Add job after unit tests:

```yaml
  accessibility:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v2
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'
      - run: pnpm install
      - name: Run axe-core accessibility scan
        run: pnpm run a11y:e2e
```

#### npm scripts (`apps/web/package.json`)

```json
{
  "scripts": {
    "a11y:lint": "eslint src/ --ext .tsx,.ts",
    "a11y:axe": "axe src/ --tags wcag2aa",
    "a11y:e2e": "playwright test e2e/accessibility-compliance.spec.ts --reporter=html"
  }
}
```

#### PR template (`.github/PULL_REQUEST_TEMPLATE.md`)

Add after existing checks:

```markdown
## Accessibility Checklist
- [ ] No new heading level skips (h1 → h3)
- [ ] Form inputs have associated labels
- [ ] Color contrast ≥ 4.5:1 for text (APCA standard for Hebrew)
- [ ] Keyboard-only navigation works (Tab, Enter, Escape)
- [ ] Error messages announce via `aria-live="polite"`
- [ ] No autofocus on page load
- [ ] Modal focus traps working (verified with Tab)
```

### Manual smoke test (15 minutes — pre-deployment only)

**Tools:** NVDA (Screen reader) + keyboard-only navigation

**Test flow:**
1. **Heading structure (3 min):** Open DevTools → Elements, verify no h2/h3 without h1, no level skips
2. **Form errors (3 min):** Submit blank form, verify error messages read aloud + highlight visible
3. **Focus traps (2 min):** Open modal, Tab through all elements, Escape closes modal
4. **Contrast (3 min):** Use APCA contrast checker for all text (especially over images); ≥ 4.5:1 for body text
5. **Keyboard nav (2 min):** Tab through full page, no focus loss, all interactive elements reachable
6. **RTL Tab order (2 min):** Verify Tab moves right-to-left in Hebrew mode

**Sign-off:** Product Manager + QA confirm before merge to production.

