import { PeopleList } from '@/features/people/components/PeopleList';

/**
 * PeoplePage: Route container for students & adults management.
 */
export default function PeoplePage() {
  return <PeopleList />;
}
