{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "lead-email-ingest",
  "main": "src/index.ts",
  "compatibility_date": "2026-08-01",
  "vars": {
    // Non-secret: the crm-lead-ingest edge function on the TARGET Supabase
    // project. Dev value committed; override per environment at deploy time.
    "CRM_INGEST_URL": "https://acmujrhavgbamdilzuew.supabase.co/functions/v1/crm-lead-ingest"
  }
  // Secret (set once per environment, never committed):
  //   npx wrangler secret put CRM_INGEST_SECRET
}
