{
  "name": "ballet-school-system",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.0.8",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "a11y:e2e": "pnpm -C apps/web run a11y:e2e"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "pnpm": "^11.0.8"
  }
}
