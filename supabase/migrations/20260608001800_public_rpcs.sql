-- =============================================================================
-- 001800: Public RPCs placeholder
-- Final definitions live later (single body each — avoid mid-chain drift):
--   get_tenant_config_by_subdomain  → 002500_feature_flag_system.sql
--   get_public_offerings_by_subdomain → 002600_scheduling.sql (offering_type = 'class')
-- DEPENDENCIES: none (no-op; kept for filename continuity in the chain)
-- =============================================================================

-- Intentionally empty. Do not recreate superseded RPC bodies here.
